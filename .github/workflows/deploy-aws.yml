name: Deploy React to AWS
on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source code
        uses: actions/checkout@v2

      - name: Generate deployment package
        run: zip -r deploy.zip . -x '*.git*'

      - name: Deploy to EB
        uses: einaregilsson/beanstalk-deploy@v21
        with:
          aws_access_key: AKIA26S3CBF6R2IPYPA5
          aws_secret_key: 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
          application_name: react-docker
          environment_name: React-docker-env
          version_label: ${{ github.sha }}
          existing_bucket_name: elasticbeanstalk-eu-north-1-752883992957
          region: eu-north-1
          deployment_package: deploy.zip
